import React from 'react';

const About: React.FC = () => {
  return (
    <div>
      {/* This component is not currently used in the app, but is here to prevent build errors. */}
    </div>
  );
};

export default About;